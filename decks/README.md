# Decks & Artifacts

Client-facing deliverables — HTML decks, one-pagers, and other rendered
artifacts ready to hand off or deploy. Distinct from `outputs/`, which
holds internal KB query answers.

Each deliverable gets its own subfolder: `decks/<client-or-project>/`.
